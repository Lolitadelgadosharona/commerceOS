# End-to-end Test Aggregation

Executable browser tests live with the owning application so Node resolves locked package dependencies. Sprint 001 web tests are in [`apps/web/tests/e2e`](../../apps/web/tests/e2e). This directory remains the repository-level index for future cross-application scenarios and evidence.
