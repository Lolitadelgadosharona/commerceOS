import { expect, test } from "@playwright/test";

const productId = "53535353-5353-4353-8353-535353535353";
test.beforeEach(async ({ request }) => { await request.post("http://127.0.0.1:4100/__scenario/success"); });
test("1 listings control center loads", async ({ page }) => { await page.goto("/listings"); await expect(page.getByRole("heading", { name: "Listing Intelligence Control Center" })).toBeVisible(); });
test("2 governed product appears", async ({ page }) => { await page.goto("/listings"); await expect(page.getByRole("heading", { name: "Portable cooling mat" })).toBeVisible(); });
test("3 listing detail opens", async ({ page }) => { await page.goto(`/listings/${productId}`); await expect(page.getByRole("heading", { name: "Portable cooling mat" })).toBeVisible(); });
test("4 Product Truth source is visible", async ({ page }) => { await page.goto(`/listings/${productId}`); await expect(page.getByRole("heading", { name: "Source Product Truth" })).toBeVisible(); });
test("5 customer problem context is visible", async ({ page }) => { await page.goto(`/listings/${productId}`); await expect(page.getByText(/Owners report recurring pet heat/)).toBeVisible(); });
test("6 commercial content is visible", async ({ page }) => { await page.goto(`/listings/${productId}`); await expect(page.getByRole("heading", { name: "Commercial Content" })).toBeVisible(); });
test("7 claim review is visible", async ({ page }) => { await page.goto(`/listings/${productId}`); await expect(page.getByRole("heading", { name: "Claim Review" })).toBeVisible(); });
test("8 supported claim source is visible", async ({ page }) => { await page.goto(`/listings/${productId}`); await expect(page.getByText(/product_truth:67676767/i)).toBeVisible(); });
test("9 unsupported claim blocks readiness", async ({ page }) => { await page.goto(`/listings/${productId}`); await expect(page.getByText("Claim is unknown: Clinically proven")).toBeVisible(); });
test("10 unknown never passes", async ({ page }) => { await page.goto(`/listings/${productId}`); await expect(page.getByText(/UNKNOWN · human review required/)).toBeVisible(); });
test("11 supported claim passes", async ({ page }) => { await page.goto(`/listings/${productId}`); await expect(page.getByText(/SUPPORTED · standard review/)).toBeVisible(); });
test("12 high risk policy is visible", async ({ page }) => { await page.goto(`/listings/${productId}`); await expect(page.getByText("Explicit policy and strong evidence required.")).toBeVisible(); });
test("13 FAQ evidence behavior is visible", async ({ page }) => { await page.goto(`/listings/${productId}`); await expect(page.getByText("supported answer", { exact: true })).toBeVisible(); });
test("14 missing policy is clear", async ({ page }) => { await page.goto(`/listings/${productId}`); await expect(page.getByText(/strong evidence required/)).toBeVisible(); });
test("15 SEO fields display", async ({ page }) => { await page.goto(`/listings/${productId}`); await expect(page.getByText("Portable pet cooling mat")).toBeVisible(); });
test("16 GEO product data displays", async ({ page }) => { await page.goto(`/listings/${productId}`); await expect(page.getByRole("heading", { name: "SEO & GEO Product Data" })).toBeVisible(); });
test("17 structured readiness displays", async ({ page }) => { await page.goto(`/listings/${productId}`); await expect(page.getByText(/Internal structured-data readiness:/)).toBeVisible(); });
test("18 backend readiness displays", async ({ page }) => { await page.goto(`/listings/${productId}`); await expect(page.getByText("NOT_READY").first()).toBeVisible(); });
test("19 warning is explicitly non blocking", async ({ page }) => { await page.goto(`/listings/${productId}`); await expect(page.getByText("WARNING — non-blocking")).toBeVisible(); });
test("20 approval and publication are separated", async ({ page }) => { await page.goto(`/listings/${productId}`); await expect(page.getByText("LISTING READY ≠ PUBLISH AUTHORIZATION")).toBeVisible(); });
test("21 listing version is visible", async ({ page }) => { await page.goto(`/listings/${productId}`); await expect(page.getByRole("heading", { name: /Listing v1/ })).toBeVisible(); });
test("22 Product Truth freshness is visible", async ({ page }) => { await page.goto(`/listings/${productId}`); await expect(page.getByText("Truth current")).toBeVisible(); });
test("23 Shopify projection is visible", async ({ page }) => { await page.goto(`/listings/${productId}`); await expect(page.getByRole("heading", { name: "Shopify Readiness Projection" })).toBeVisible(); });
test("24 no external Shopify id exists", async ({ page }) => { await page.goto(`/listings/${productId}`); await expect(page.getByText("External Shopify ID")).toBeVisible(); await expect(page.getByText("UNKNOWN / not provided").last()).toBeVisible(); });
test("25 Build links to Listing", async ({ page }) => { await page.goto(`/build/${productId}`); await expect(page.getByRole("link", { name: "Open Listing workspace →" })).toHaveAttribute("href", `/listings/${productId}`); });
